<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= project.title %> - Climate Solutions</title>
  <link href="/css/main.css" rel="stylesheet">
</head>
<body class="bg-base-100 text-base-content">
  
  <%- include('partials/navbar', {page: ''}) %>

  
  <div class="hero min-h-[50vh] bg-base-200">
    <div class="hero-content text-center">
      <div class="max-w-2xl">
        <h1 class="text-5xl font-bold mb-4"><%= project.title %></h1>
        <p class="py-6 text-xl">Below, you will find detailed information about the project: <%= project.title %></p>
        <div class="mt-8">
          <p class="text-sm"><%= studentName %> | Student ID: <%= studentId %></p>
          <p class="text-xs opacity-75">Current time: <%= timestamp.toLocaleString() %></p>
        </div>
      </div>
    </div>
  </div>

  
  <div class="container mx-auto p-8">
    <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
      
      <div class="flex justify-center">
        <img src="<%= project.feature_img_url %>" alt="<%= project.title %>" class="rounded-lg shadow-lg w-full">
      </div>
      
      <div class="space-y-4">
        <h2 class="text-3xl font-bold">Project Overview</h2>
        <p class="text-lg"><%= project.intro_short %></p>
        <h3 class="text-2xl font-bold">Impact</h3>
        <p class="text-lg"><%= project.impact %></p>
        <a href="<%= project.original_source_url %>" class="btn btn-primary" target="_blank">Learn More</a>
        <a href="#" onclick="history.back(); return false;" class="btn btn-secondary">Back to Projects</a>
      </div>
    </div>
  </div>

 
  <div class="container mx-auto p-8 text-center">
    <blockquote class="text-xl italic">
      "<span id="quote"></span>"
      <footer class="mt-2 text-sm opacity-75">- <span id="author"></span></footer>
    </blockquote>
  </div>

  <script>
    
    document.addEventListener("DOMContentLoaded", () => {
      fetch("https://dummyjson.com/quotes/random")
        .then(response => response.json())
        .then(data => {
          document.getElementById("quote").textContent = data.quote;
          document.getElementById("author").textContent = data.author;
        })
        .catch(error => {
          console.error("Error fetching quote:", error);
          document.getElementById("quote").textContent = "Stay curious and keep learning!";
          document.getElementById("author").textContent = "Anonymous";
        });
    });
  </script>
</body>
</html>